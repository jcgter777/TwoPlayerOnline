/*function genCode() {
    location.replace("play.html");
    var randNum = Math.round(Math.random()*90001+10000);
    //document.getElementById("session").innerHTML = randNum;
}*/

function search(ele) {
    if(event.key === 'Enter') {
        alert(ele.value);
    }
}